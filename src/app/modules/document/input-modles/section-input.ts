import SectionSetup from "../section-setup";
import ComponentInput from "./component-input";

export default interface SectionInput {
    sectionSetup:SectionSetup,
    componentInputs:ComponentInput[],
}